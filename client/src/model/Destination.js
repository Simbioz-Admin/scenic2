"use strict";

define( [
    'underscore',
    'backbone'
], function ( _, Backbone ) {

    /**
     *  @constructor
     *  @augments module:Backbone.Model
     */
    var Destination = Backbone.Model.extend( {
        defaults:    {

        },

        initialize: function () {

        }
    } );

    return Destination;
} );
