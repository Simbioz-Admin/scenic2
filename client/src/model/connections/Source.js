"use strict";

define( [
    'underscore',
    'backbone'
], function ( _, Backbone ) {

    /**
     * Source
     *
     * @constructor
     * @extends module:Backbone.Model
     */
    var Source = Backbone.Model.extend( {

        /**
         * Initialize
         */
        initialize: function () {

        }
    } );

    return Source;
} );
