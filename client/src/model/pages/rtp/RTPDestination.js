"use strict";

define( [
    'underscore',
    'backbone'
], function ( _, Backbone ) {

    /**
     * RTP Destination
     *
     * @constructor
     * @extends module:Backbone.Model
     */
    var RTPDestination = Backbone.Model.extend( {

    } );

    return RTPDestination;
} );
