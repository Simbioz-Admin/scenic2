"use strict";

define( [
    'underscore',
    'backbone'
], function ( _, Backbone ) {

    /**
     * Method Argument
     *
     * @constructor
     * @extends module:Backbone.Model
     */
    var Argument = Backbone.Model.extend( {

        /**
         * Initialize
         */
        initialize: function () {

        }
    } );

    return Argument;
} );
