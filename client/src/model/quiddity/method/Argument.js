"use strict";

define( [
    'underscore',
    'backbone'
], function ( _, Backbone ) {

    /**
     *  @constructor
     *  @augments module:Backbone.Model
     */
    var Argument = Backbone.Model.extend( {
        defaults:    {

        },

        initialize: function () {

        }
    } );

    return Argument;
} );
