

module.exports = function(_, switcher) {

	//console.log("loading quidds for test");
 
	// //create default for test interface
	// for (i = 0; i < 1; i++) 
	// { 
	//    var quidd = switcher.create("videotestsrc");
	//    switcher.subscribe_to_property(quidd, "shmdata-writers");
	//    switcher.set_property_value(quidd, "started", "true");
	// } 

	// for (i = 0; i < 1; i++) 
	// { 
	//    var quidd = switcher.create("audiotestsrc");
	//    switcher.subscribe_to_property(quidd, "shmdata-writers");
	//    switcher.set_property_value(quidd, "started", "true");
	// } 

	// //destination
	// for (i = 0; i < 1; i++) 
	// { 
	// 	switcher.invoke("defaultrtp", "add_destination", ["destination-"+i, "destination-"+i]);
	// }
}